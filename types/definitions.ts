export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
  auth_key: string;
};

export type ProjectTable = {
  id: string;
  name: string;
  website_url: string;
};


export type LessonsTable = {
  id: string;
  name: string;
  path: string;
};


export type AccountsTable = {
  id: string;
  name: string;
  email: string;
};


